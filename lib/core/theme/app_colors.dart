import 'package:flutter/material.dart';

class AppColors {
  // Common Colors
  static const Color primaryNeon = Color(0xFF39FF14);
  static const Color primaryNeonLight = Color(0xFF7FFF00);
  static const Color primaryNeonDark = Color(0xFF2EB311);
  static const Color error = Color(0xFFFF3333);

  // Dark Theme Palette
  static const Color background = Color(0xFF000000);
  static const Color backgroundDarkGreen = Color(0xFF0A1F0A);
  static const Color cardBackground = Color(0x3300FF00); // 20% opacity green
  static const Color cardBorder = Color(0x8039FF14); // 50% opacity neon
  static const Color textLight = Color(0xFFFFFFFF);
  static const Color textMuted = Color(0xFFB0D0B0);

  // Light Theme Palette (Pure Studio White)
  static const Color backgroundLight = Color(0xFFFFFFFF); // Pure White Canvas
  static const Color cardLight = Color(0xFFFFFFFF);
  static const Color borderLight = Color(0xFFF3F4F6); // Soft interface divider
  static const Color textDark = Color(0xFF111827); // Deep Charcoal
  static const Color textMutedDark = Color(0xFF6B7280); // Secondary text
  static const Color accentEmerald = Color(0xFF00875A); // Sophisticated secondary accent
}
